# Диагностика OSD: RSSI=0%, LQ=0%, неверный заряд батареи

Анализ скриншота OSD и файла параметров `GR_ERLS_v28c`.

---

## ПРОБЛЕМА 1: RSSI и LQ показывают 0%

### Что видно на экране
- RSSI = **0%** (позиция X=39, Y=3)
- LQ = **0%** (позиция X=39, Y=5)
- При этом RC-управление **работает** (режим ACRO активен, стики отвечают)

### Почему так происходит
Параметры ArduPilot **настроены правильно** (`RSSI_TYPE=3`, `RC_OPTIONS=768`, `SERIAL4_BAUD=420`).
Проблема на стороне **ELRS-приёмника** или **физического подключения**.

RC-управление работает через CRSF без телеметрии (однонаправленно). А вот **Link Statistics** (откуда ArduPilot берёт RSSI/LQ) передаются в составе телеметрического обмена — и именно этот обмен не работает.

### Причины и решения

#### Причина А: Телеметрия ELRS выключена или слишком редкая
1. Открой **ELRS Configurator** (через WiFi приёмника или ELRS Lua скрипт на пульте)
2. Проверь **Telemetry Ratio** — должен быть **1:2**, **1:4**, **1:8**, **1:16** или **1:32** (НЕ OFF!)
3. Рекомендация: **1:16** для баланса задержки и телеметрии

#### Причина Б: Перепутаны TX/RX провода на SERIAL4
```
Правильное подключение:
┌──────────────┐     ┌──────────────┐
│  Cube Orange │     │ ELRS Receiver│
│              │     │              │
│  SERIAL4 TX ─┼─────┼─ RX          │
│  SERIAL4 RX ─┼─────┼─ TX          │
│  GND ────────┼─────┼─ GND         │
│  5V ─────────┼─────┼─ 5V          │
└──────────────┘     └──────────────┘
```
- Если подключён **только один провод** (RX←TX) — RC работает, но телеметрия нет
- Нужны **ОБА провода**: TX→RX и RX←TX

#### Причина В: Baud rate приёмника ≠ 420000
1. В **ELRS Configurator** → раздел UART
2. Baud Rate должен быть **420000**
3. Если стоит 460800 или 921600 — сменить на 420000
4. Должен совпадать с `SERIAL4_BAUD=420` в ArduPilot

#### Причина Г: Нужен power cycle
- После изменения параметров — **полное отключение питания** (вытащить батарею + USB)
- Простая перезагрузка не всегда применяет изменения SERIAL/CRSF

### Как проверить в Mission Planner
1. Подключить Cube Orange
2. Перейти в **Flight Data → Status** (внизу)
3. Найти поля:
   - `rxrssi` — должен быть > 0 (обычно 0.XX)
   - `rssi` — должен быть > 0
4. Если оба = 0 → проблема в связке ELRS↔SERIAL4 (провода или baud)
5. Если > 0 в Status, но 0 на OSD → проблема в Walksnail/MSP (маловероятно)

---

## ПРОБЛЕМА 2: Неправильный заряд батареи

### Что видно на экране
- Напряжение: **25.1V**
- Напряжение ячейки: **4.19V**

### Несоответствие в параметрах

| Параметр | Значение | Что означает |
|----------|----------|-------------|
| `MOT_BAT_VOLT_MAX` | **50.4** | 12S батарея (12 × 4.2V) |
| `MOT_BAT_VOLT_MIN` | **36** | 12S батарея (12 × 3.0V) |
| `OSD_CELL_COUNT` | **6** | OSD считает что 6S |

**Это противоречие!** Либо батарея 12S, либо 6S — нужно определить.

### Вариант A: Батарея реально 12S

Если у тебя **12S батарея** (полный заряд ~50.4V), то OSD показывает **вдвое заниженное** напряжение:
- Реальное: ~50.2V → на экране: 25.1V
- Причина: `BATT_VOLT_MULT` = 47.564 — **вдвое меньше нужного**

**Исправления:**
```
BATT_VOLT_MULT = 95.128       (47.564 × 2)
OSD_CELL_COUNT = 12
```

### Вариант B: Батарея реально 6S

Если у тебя **6S батарея** (полный заряд ~25.2V), то напряжение на экране правильное, но пороги мотора неверны:

**Исправления:**
```
MOT_BAT_VOLT_MAX = 25.2       (6 × 4.2V)
MOT_BAT_VOLT_MIN = 19.8       (6 × 3.3V)
```

### Как определить
1. **Мультиметром** измерь напряжение на разъёме батареи
2. Посчитай ячейки: 12S = 50.4V полный, 6S = 25.2V полный
3. Либо посмотри маркировку на батарее (6S / 12S)

---

## ПРОБЛЕМА 3: Дополнительные замечания

### GPS отключён
- `GPS_TYPE = 0` — GPS выключен
- `AHRS_GPS_USE = 0` — навигация без GPS
- На OSD видна 1 иконка спутника — это нормально при выключенном GPS

### Failsafe настроен на No PWM
- `FS_THR_ENABLE = 3` — failsafe по потере сигнала (Throttle PWM failsafe)
- `FS_THR_VALUE = 975` — порог
- Это правильно для ELRS

### ARMING_CHECK = 0
- **Все проверки армирования отключены** — будь осторожен
- Рекомендуется включить хотя бы базовые: `ARMING_CHECK = 1`
